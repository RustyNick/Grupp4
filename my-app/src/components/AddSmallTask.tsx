import React, { useState } from 'react'
import { initialTodos } from './ViewPage'


/* export const AddSmallTask = () => {
    initialTodos.filter((task) => {
        const smallTask = task.task
        smallTask.filter((task) => {
            const theTask = task.textName





            const [tasks, SetTasks] = useState(theTask)


            const removeTask: RemoveTask = taskToDelete => {
                SetTasks(tasks.filter((task) => {
                    return task !== taskToDelete
                }))

            }

        })
    })



    return (
        <div>

        </div>
    )


} */